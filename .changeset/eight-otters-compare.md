---
"trackio": minor
---

feat: Run comparer panel on the Metrics page — compare run configs and metadata across selected runs side by side, with a diff-only toggle, key search, per-cell copy, and highlighting of differing rows.
